service:
  image: riptide_integration_test
  port: 8080
  roles:
    - src
  logging:
    stdout: true
    stderr: true
    paths:
      one: /default_workdir/logging_test
    commands:
      two: echo "1 2 3 4 this is command logging test"
