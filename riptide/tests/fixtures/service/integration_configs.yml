service:
  image: riptide_integration_test
  port: 8080
  config:
    one:
      $source: foo
      from: configs/config1.txt
      to: /config1
    two:
      $source: foo
      from: configs/config2.txt
      to: /config2